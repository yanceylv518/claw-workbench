# Notion 双库设计

## 目标

把现有的 Notion 结构拆成两张库：

1. `AI 情报库`
2. `内容发布库`

这样可以把“原始情报素材”和“加工后的内容成品”分开管理，再用 Relation 关联起来。

## 一、AI 情报库

这张库继续负责存原始线索和素材。

### 建议字段

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| 标题 | Title | 情报标题 |
| 总结 | Rich text | 一句话总结 |
| 用途 | Rich text | 为什么有用 |
| 链接 | URL | 原始来源链接 |
| 日期 | Date | 发布时间或采集时间 |
| 分类 | Select | `AI情报` / `自媒体选题` / `行业情报` / `财经情报` |
| 状态 | Select | `待处理` / `已采用` / `已归档` |
| 优先级 | Select | `高` / `中` / `低` |
| 已生成内容 | Relation | 指向 `内容发布库` |
| 内容数量 | Rollup | 统计关联内容数 |

### 作用

- 放原始新闻、线索、素材
- 记录它们后来有没有被做成内容

## 二、内容发布库

这张库负责存最终内容产物，比如小红书发布包、公众号稿、短视频脚本。

### 建议字段

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| 标题 | Title | 内容标题 |
| 内容类型 | Select | `小红书发布包` / `公众号` / `短视频脚本` / `朋友圈` / `选题卡` |
| 发布状态 | Select | `已生成` / `待审核` / `待发布` / `已发布` / `废弃` |
| 来源情报 | Relation | 指向 `AI 情报库` |
| 摘要 | Rich text | 一句话说明内容主题 |
| 目标平台 | Select | `小红书` / `公众号` / `抖音` / `视频号` / `多平台` |
| 发布日期 | Date | 计划发布时间 |
| 发布链接 | URL | 实际发布后链接 |
| 优先级 | Select | `高` / `中` / `低` |
| 封面文案 | Rich text | 可选 |
| 推荐话题 | Rich text | 可选 |

### 页面正文建议内容

- 最终发布正文
- 配图清单
- 发布检查清单
- 首条评论建议
- 置顶评论建议
- 参考素材

## 三、两个库怎么关联

推荐做成双向 Relation：

- `AI 情报库.已生成内容` -> `内容发布库`
- `内容发布库.来源情报` -> `AI 情报库`

### 实际含义

- 一条情报可以被多篇内容复用
- 一篇内容也可以引用多条情报

这就是一个标准的多对多关联。

## 四、最推荐的视图

### AI 情报库

1. `全部情报`
2. `待处理`
3. `已采用`
4. `按分类查看`

### 内容发布库

1. `待发布`
2. `待审核`
3. `已生成`
4. `按平台查看`
5. `按内容类型查看`

## 五、当前代码支持情况

现在代码已经支持：

- 如果 `content_publish.database_id` 已配置：
  - 小红书发布包优先写入 `内容发布库`
  - 并尝试按来源链接/标题去 `AI 情报库` 查找对应页面
  - 找到后自动写入 `来源情报` Relation

- 如果 `content_publish.database_id` 还没配置：
  - 仍然回退写入现有 `AI 情报库`

## 六、你现在需要手动做的事

由于当前 Notion 集成只能直接看到 `AI 情报` 这个数据库，看不到它的父页面，所以代码这边暂时不能自动替你新建第二张数据库。

你需要在 Notion 里手动新建一张库，建议名：

- `内容发布库`

并至少创建下面这些字段：

- `标题`
- `内容类型`
- `发布状态`
- `来源情报`
- `摘要`
- `目标平台`
- `发布日期`
- `发布链接`

其中：

- `来源情报` 必须是 Relation
- 并且要关联到 `AI 情报库`

## 七、建好后怎么接入

把新数据库的 ID 填到：

- [D:\openclaw\notion-ai-intel.config.json](D:/openclaw/notion-ai-intel.config.json)

字段：

```json
"content_publish": {
  "token": "",
  "database_id": "这里填你的内容发布库数据库ID",
  "property_map": {
    "title": "标题",
    "content_type": "内容类型",
    "publish_status": "发布状态",
    "source_intel": "来源情报",
    "summary": "摘要",
    "platform": "目标平台",
    "publish_date": "发布日期",
    "publish_url": "发布链接"
  }
}
```

填好后，桥重启一次即可生效。
